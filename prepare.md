# 王雁林 面试拷打清单

> 简历定位：AI Agent / 后端架构工程师，字节跳动 3 年（含实习），主攻 AIOps Agent + 大型分布式系统稳定性。
> 拷打思路：简历里大量结论性、量化、"公司最早/最好用"类措辞，需逐条让其还原**技术细节、归因、个人贡献边界、踩坑与权衡**。

---

## 一、AI Agent 方向（重点中的重点，简历主线）

### 1. 告警诊断 Agent —— "10min → 3min(p99)，准确率 95%"
这是最容易被夸大的数字，重点逼问归因和方法论。

- **基线怎么来的？** "10 分钟以上" 是人工诊断的什么环节？是 MTTI（定位）还是 MTTR（恢复）？3min(p99) 的口径是 Agent 端到端响应，还是只到"给出结论"？两个数字口径是否一致、可比？
- **准确率 95% 如何定义和测量的？** 分子分母是什么？多少样本？谁来标注 ground truth？是离线评测集还是线上抽样？召回 vs 精确率分别是多少？误诊（false positive）的代价是什么，有没有兜底？
- **诊断的本质是什么？** 是 LLM 在"推理"，还是把专家经验固化成了 workflow / 决策树，LLM 只做了自然语言包装？如果是后者，Agent 的价值到底在哪、为什么不用规则引擎？
- **召回的上下文从哪来？** 监控指标、日志、trace、变更记录如何接入？token 爆炸怎么处理？长上下文如何裁剪/排序？检索的 recall 不够会怎样？
- **幻觉与误导问题**：诊断 Agent 给出错误根因导致 oncall 走错方向，这种 case 出现过吗？怎么发现、怎么收敛？
- **EB 级存储**具体是哪个系统？告警类型有多少种？Agent 覆盖了其中多少比例？没覆盖的怎么办？
- "被多个兄弟部门复用"——复用的是 prompt、框架、还是诊断知识？知识是存储领域强相关的，别人怎么复用？

### 2. TOS 智能助理 Agent —— Text2SQL + RAG + 知识库 + 日志 + 基础信息查询
- **Text2SQL 的准确率和安全性**：生成的 SQL 跑错库/全表扫描/拖垮线上库怎么防？有没有 schema linking、SQL 校验、只读账号、超时与行数限制？复杂多表 join、聚合场景的准确率？
- **多数据源如何编排？** 一个问题需要同时查日志 + SQL + 知识库时，谁来决定调用顺序？是 LLM 规划（plan）还是固定 pipeline？多源结果冲突怎么裁决？
- **RAG 细节**：切分策略、embedding 模型、向量库选型、检索 topK、重排（rerank）有没有？知识库怎么更新、过期知识怎么处理？检索不到时的行为？
- **"200+ 用户，100+ 活跃，日均 40+ 会话"**——日均 40 会话 / 100 活跃用户 = 人均 0.4 次/天，这个活跃度算高吗？怎么定义"活跃"？留存如何？有没有用户用一次就不用了的情况，为什么？
- **"2025 存储团队最好用的工具"**——谁评的？评选标准？还是内部口碑说法？逼问数据支撑。
- 用户问的问题里，Agent 答错或答不了的占比多少？bad case 怎么收集和迭代？

### 3. Agent 框架与核心技术（简历列了一长串，挨个验真）
- 简历同时写了 **LangChain、LangGraph、Eino、ADK**——这几个在实际项目里**各自用在哪**？还是只是"了解过"？让他对比 LangGraph 与 Eino 的设计差异（状态管理、图编排、流式）。
- **Eino**（字节自研）：为什么内部项目还用 LangChain/LangGraph？生产落地到底用的哪个？混用的理由？
- **ReAct**：在你的 Agent 里 ReAct 的 loop 实际跑几轮？无限循环 / 反复调用同一工具怎么兜底？为什么用 ReAct 而不是 Plan-and-Execute？
- **Function Call / MCP**：MCP 在你项目里解决了什么 LangChain tool 解决不了的问题？工具数量多时（几十上百个）怎么做工具选择/路由？
- **"Agent 后训练、Action Trace 回流、数据飞轮"**——这是最硬核也最容易吹的点：
  - 后训练是 SFT 还是 RLHF/DPO？谁来做的数据、谁来训、用什么基座、训了几个版本？**你个人在其中做了什么具体的事**？
  - Action Trace 怎么回流？trace 的标注怎么做的？数据量级？飞轮"转起来"的标志是什么，有没有指标证明模型迭代后效果提升？
  - 如果只是"设计了方案但没真正训练落地"，要明确边界。

### 4. 通用 Agent 灵魂拷问
- LLM 调用的成本和延迟如何控制？用的什么模型？为什么？有没有做模型降级/小模型分流？
- Agent 的评测体系怎么建？除了人工，有没有自动化 eval？
- 多轮对话的状态/记忆怎么管理？长会话如何处理？
- 线上稳定性：LLM 服务超时/限流时 Agent 怎么降级？

---

## 二、稳定性组（2023.7–2025.2，分布式系统硬功夫）

### 5. "故障数从一年 4~6 次收敛到 0~1 次"
- 这 4~6 次故障的**根因分布**是什么？是不是都能归因于"流量失控"？归因是否过度简化？
- 0~1 次是哪一年的数据？时间窗口对齐了吗（治理前 vs 治理后是否同口径同周期）？
- 这个收敛**有多少是你的组件带来的**，多少是大盘流量本身变化、其他团队的工作？怎么剥离归因？

### 6. 分布式流控（令牌桶）
- **分布式令牌桶的核心难题是全局配额的分配与同步**：你是中心化发令牌还是本地预分配 + 周期对账？强一致还是最终一致？
- 中心节点挂了怎么办（单点）？网络分区时宁可错杀还是放过？
- **"贴近业务的流控算法"**具体贴近在哪？多租户/多优先级/突发流量（burst）怎么处理？和标准令牌桶/漏桶的区别？
- 时钟漂移、令牌补充粒度、热点 key 倾斜怎么处理？
- 为什么不直接用 Sentinel / 现成的分布式限流中间件，要自研？

### 7. 其他稳定性手段（业务分组调度、镜像流量灰度、Server 侧配额缓存、3 级降级）
- **Server 侧配额缓存**：缓存一致性怎么保证？配额是强一致还是允许超发？缓存击穿/雪崩防护？
- **镜像流量灰度**：怎么做的流量复制？读写分离吗？写请求镜像会不会污染线上数据？mismatch 怎么对比和告警？
- **流控 3 级降级**：三级分别是什么？触发条件、自动还是手动、降级后的用户体验、恢复机制？降级误触发过吗？
- 这些组件你是**主 owner 还是参与者**？设计是你提的还是 follow 别人方案？

---

## 三、GPU 池化计算实习（2022，底层硬核）

### 8. nvidia-smi ioctl Hook
- Hook 在哪一层做的？LD_PRELOAD 拦 libnvidia-ml？还是更底层拦 /dev/nvidia* 的 ioctl 系统调用？
- **为什么要 Hook nvidia-smi**？池化场景下要解决的具体问题是什么（隔离显存视图？伪造拓扑？算力切分？）？
- ioctl 的 cmd 那么多，你怎么逆向出需要 hook 哪些、参数结构是什么（NVIDIA 闭源）？怎么调试的（gdb / strace）？
- 不同显卡型号、不同 driver 版本兼容性怎么保证？升级 driver 后 hook 失效怎么办？这恰恰是简历说的"保障兼容性"，要逼问具体方法。
- "异构算力池化、隔离与流控"——显存隔离和算力（SM）隔离分别怎么实现？是 MPS / MIG 还是自研 hook？

---

## 四、学术 / 算法背景（硕士论文 SOTA）

### 9. 基于视频的人体姿态估计 + Transformer，"算法效果达到 SOTA"
- SOTA 是在哪个 **benchmark/数据集**上、什么**指标**（mPJPE / PCK / AP）？对比了哪些方法？SOTA 是某个子指标还是整体？
- 用 Transformer 做视频姿态估计，你的创新点 vs 已有工作（如 PoseFormer、VideoPose3D）的差异？
- Transformer 在时序建模上相比 TCN/LSTM 的优势？计算量怎么权衡？
- 现在还跟得上 CV/姿态估计的进展吗？（试探知识是否过时，但也别太苛刻）

---

## 五、横向 / 综合能力拷问

### 10. AI Coding 方法论
- 简历说"形成一套 AI Coding 方法论并在公司推广"——**这套方法论核心是什么**？能讲 3 条可落地的原则吗？
- Claude Code / Codex 在你日常开发中具体怎么用？哪些场景不适合用？怎么保证 AI 生成代码的质量和安全？

### 11. 角色与协作
- "Agent 小组负责人 / 方向 POC"——带了几个人？你做管理多还是写代码多？技术决策最终拍板的是你吗？
- 跨团队推广时遇到的最大阻力是什么？怎么解决的？
- 简历里大量"公司内最早""被多部门复用""最好用"——**这些自我评价，有没有第三方/数据能佐证**？（关注是否有自我夸大倾向）

### 12. 技术深度兜底（防止"什么都会一点，什么都不深"）
- 让他选**最有成就感、技术最深**的一个项目，从头到尾讲架构、讲他个人写的关键代码、讲最难的 bug。
- Golang：GMP 调度、GC、channel/锁的取舍、内存逃逸——挑一两个深问。
- 数据结构算法：简历强调"扎实基础"，可现场出一道中等难度题验证。

---

## 六、可能的"水分"信号汇总（面试官重点核实）
1. 一堆量化指标（3min/95%/200+/0~1 次）——**口径、基线、归因**全要追。
2. "公司内最早""被多部门复用""最好用工具"——荣誉性表述，要数据或他人佐证。
3. "Agent 后训练 + 数据飞轮"——是否真落地到模型训练，还是停留在方案设计。
4. 框架罗列过多（LangChain/LangGraph/Eino/ADK）——区分"精通"与"用过/听过"。
5. 个人贡献 vs 团队成果的边界——反复用"我具体做了什么、哪行代码是你写的"来收敛。

---

## 面试节奏建议
- 先用 **告警诊断 Agent** 切入（主线、量化最多），用归因和口径问题打开缺口。
- 再下沉到 **流控/稳定性** 验证后端硬功夫。
- 用 **nvidia-smi Hook** 和 **论文 SOTA** 验证底层/算法深度。
- 最后用 **AI Coding 方法论 + 个人贡献边界** 评估表达与诚信度。
